using System;
using Accord.Math;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace MathCore
{
    public static class Distance
    {
        //public static double Euclidean(double[] a, double[] b)
        //{
        //    return Math.Sqrt(a.Subtract(b).SquaredLoss().Sum());
        //}

        //public static double Euclidean(double[] a, int[] b)
        //{
        //    return Math.Sqrt(a.Subtract(b).SquaredLoss().Sum());
        //}
    }
}
